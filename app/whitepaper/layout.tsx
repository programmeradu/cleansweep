import type React from "react"
export const metadata = {
  title: "CleanSweep Whitepaper - Technical Documentation",
  description:
    "Technical documentation and architecture overview of the CleanSweep platform built on Reactive Network.",
}

export default function WhitepaperLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}

